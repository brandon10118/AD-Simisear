services:
  ad-simisear:
    build:
      context: .
    container_name: ad-simisear-service
    ports:
      - "6090:8000"
    develop:
      watch:
        - path: .
          target: /var/www/html
          action: sync
        - path: ./composer.json
          action: rebuild
        - path: ./composer.lock
          action: rebuild
    depends_on:
      - postgresql
      - mongodb

  mongodb:
    image: mongo:latest
    container_name: ad-simisear-mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: rootPassword
      MONGO_INITDB_DATABASE: simisear-mongodb
    ports:
      - "26969:27017"
    volumes:
      - ad-simisear-mongodb:/data/db/mongodb

  postgresql:
    image: postgres:15
    container_name: ad-simisear-postgresql
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: simisear-postgresql
    ports:
      - "5269:5432"
    volumes:
      - ad-simisear-postgresql:/data/db/postgresql

volumes:
  ad-simisear-mongodb:
    driver: local
  ad-simisear-postgresql:
    driver: local
